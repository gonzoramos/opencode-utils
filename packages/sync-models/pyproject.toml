[project]
name = "sync-models"
version = "0.1.0"
description = "Sync opencode.json provider models with local oMLX / Ollama servers."
requires-python = ">=3.11"
dependencies = []

[project.scripts]
sync-models = "sync_models.cli:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
