[project]
name = "opencode-utils"
version = "0.1.0"
description = "Monorepo of small OpenCode utilities."
requires-python = ">=3.11"
dependencies = [
    "sync-models",
]

[tool.uv.workspace]
members = ["packages/*"]

[tool.uv.sources]
sync-models = { workspace = true }

[tool.uv]
package = false
